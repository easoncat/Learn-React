import React, { memo } from 'react'
import AuthForm from '../Components/Auth/AuthForm'

const AuthPage = memo(() => {
  return (
    <div>
        <AuthForm></AuthForm>
    </div>
  )
})

export default AuthPage